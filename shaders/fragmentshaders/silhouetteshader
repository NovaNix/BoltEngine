#version 330

uniform sampler2D sampler;
uniform vec3 SilhouetteColor = new vec3(0, 0, 0);

in vec2 TexCoord;

out vec4 Color;

void main()
{
	vec4 TexColor = texture(sampler, TexCoord);

	if (TexColor.w == 0) discard;
	
	Color = new Vec4(SilhouetteColor, TexColor.w);

}